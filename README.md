# PostgreSQL Commands - সম্পূর্ণ গাইড (বাংলা)

## 🔹 ডাটাবেস সংক্রান্ত কমান্ড

| কমান্ড | ব্যাখ্যা |
|-------|----------|
| `\l` | সার্ভারে কয়টি ডাটাবেস আছে তা দেখায় |
| `\c database_name` | এক ডাটাবেস থেকে অন্য ডাটাবেসে চলে যায় |
| `create database database_name;` | নতুন ডাটাবেস তৈরি করে |
| `\conninfo` | কোন ডাটাবেসে কানেক্টেড আছো তা দেখায় |

## 🔹 টেবিল সংক্রান্ত কমান্ড

| কমান্ড | ব্যাখ্যা |
|-------|----------|
| `\dt` | বর্তমান ডাটাবেসে কয়টি টেবিল আছে তা দেখায় |
| `select * from table_name;` | নির্দিষ্ট টেবিলের সব ডেটা দেখায় |

## 🔹 ইউজার ও ইনফরমেশন সংক্রান্ত কমান্ড

| কমান্ড | ব্যাখ্যা |
|-------|----------|
| `\du` | ডাটাবেসে কয়জন ইউজার আছে তা দেখায় |
| `select version();` | PostgreSQL এর বর্তমান ভার্সন দেখায় |
| `select current_date;` | বর্তমান তারিখ দেখায় |

## 🔹 অন্যান্য দরকারি কমান্ড

| কমান্ড | ব্যাখ্যা |
|-------|----------|
| `\l cls` | কমান্ড লাইন স্ক্রিন ক্লিয়ার করে |

## ⚙️ psql shell ছাড়া অন্য টার্মিনালে PostgreSQL চালানো

```bash
psql -U postgres -d postgres
```

- `-U` → কোন **user** ব্যবহার করা হবে (যেমন `postgres`)  
- `-d` → কোন **database** এ প্রবেশ করবে (যেমন `postgres`)  

## ⚠️ যদি Error দেখায়

```text
psql : The term 'psql' is not recognized as the name of a cmdlet, function, script file, or operable program...
```

- অর্থাৎ তোমার **PostgreSQL এর path Environment Variable এ সেট করা নেই।

## ✅ Path ঠিক করার পদ্ধতি

1. **This PC → Program Files → PostgreSQL → Version folder** এ যাও  
2. **bin** ফোল্ডারে ঢুকো  
3. Path কপি করো  
4. **System Environment Variables → Path → New** → কপি করা Path পেস্ট করো  
5. সব OK দিয়ে বের হও  
6. নতুন CMD বা PowerShell খুলে লিখো:

```bash
psql -U postgres
```

এবার PostgreSQL ঠিকভাবে চলবে।

## 🎯 টিপস

- নতুন ডাটাবেস তৈরি করার পর সবসময় `\c database_name` দিয়ে সেই ডাটাবেসে ঢুকে কাজ করো।  
